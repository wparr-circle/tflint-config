on:
  push:
    branches:
      - main
      
name: test

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        git tag -s -d test
        git push origin :test
